{
  "name": "just-ai-help",
  "version": "0.1.0",
  "description": "Help-docs and translation tooling for any app that keeps its strings in standard i18n JSON. Translates a locale folder with an AI engine (local or online), and authors the help/hints system from the same source.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "just-ai-help": "./src/translate.js"
  },
  "scripts": {
    "translate": "node src/translate.js",
    "extract": "node src/extract.js",
    "review": "node src/review.js",
    "test": "node --test",
    "build:ui": "cd review-ui && npm run build && cd .. && node scripts/ui-hash.js"
  },
  "engines": {
    "node": ">=24"
  }
}
